services:
  dev:
    image: molaccess/fedora-release:latest
    build:
      dockerfile: ./Dockerfile
    volumes:
      - type: bind
        source: ../../../../.
        target: /opt/workdir
      - type: bind
        source: ../../../../installdir
        target: /opt/installdir